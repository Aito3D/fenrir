# backend/app/services/aito_manual_payments.py  (stub, completed in Task 8)
from sqlalchemy.ext.asyncio import AsyncSession


async def refresh_after_payment(db: AsyncSession, project_id: int, kind: str) -> None:
    """Filled in Task 8."""
    return None
